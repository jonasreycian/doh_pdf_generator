import 'package:flutter/material.dart';

class HealthQuestionScreen extends StatelessWidget {
  static final String routeName = "/health_question";

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}