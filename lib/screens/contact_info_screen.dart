import 'package:flutter/material.dart';

class ContactInformationScreen extends StatelessWidget {
  static final String routeName = "/contact_info";

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}